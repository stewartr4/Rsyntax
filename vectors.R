# use the c() function to create a vector values 

people <- c("Sarah", "Art", "John")
people

people_length <- length(people)
people_length

numbers <- c(1,2,3,4,5)
numbers

# use the seq() function to create a vector of numbers 1 through 70 
one_to_seventy <- seq(1, 70)
print(one_to_seventy)

odds <- seq(1, 10, 2)
print(odds)

one_to_eighty <- 1:80 # seq() shorthand call 
print(one_to_eighty)